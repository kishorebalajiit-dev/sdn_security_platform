version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: securenet_ai
      POSTGRES_USER: securenet
      POSTGRES_PASSWORD: securenet
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  ganache:
    image: trufflesuite/ganache:v7.9.0
    command: --host 0.0.0.0 --port 8545 --mnemonic "securenet development key mnemonic" --accounts 10 --defaultBalanceEther 100
    ports:
      - "8545:8545"

  ryu:
    build:
      context: ./ryu_controller
      dockerfile: Dockerfile
    ports:
      - "6633:6633"
      - "8080:8080"
    volumes:
      - ./ryu_controller:/app

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file:
      - .env
    environment:
      SECRET_KEY: change-me
      JWT_SECRET_KEY: change-me-too
      GANACHE_URL: http://ganache:8545
      RYU_API_URL: http://ryu:8080
      RYU_CONTROLLER_URL: http://ryu:8080
    depends_on:
      - ganache
      - ryu
    ports:
      - "5000:5000"

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      - VITE_API_BASE_URL=http://localhost:5000/api/v1
    depends_on:
      - backend
    ports:
      - "5173:5173"

  mininet:
    build:
      context: ./mininet
      dockerfile: Dockerfile
    privileged: true
    network_mode: host
    volumes:
      - /lib/modules:/lib/modules
    depends_on:
      - ryu
      - backend

volumes:
  postgres_data:
